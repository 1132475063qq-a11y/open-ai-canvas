# Image Model Routing Profile

This profile is derived from the user-supplied LIRA material and is not treated as current vendor truth without runtime evidence.

Suggested lanes from that source:
- character/casting → Soul 2.0 / AI Cast profile
- location/cinematic still → Soul Cinema profile
- prop/product-style object → NBP / GPT Image profile
- existing-frame edit → NBP-first profile
- texture cleanup → Seedream texture-pass profile
- finest local surgery / some location view changes → GPT Image profile

When platform/model capability is not verified in the current project, mark the route UNVERIFIED_PROFILE and keep a generic fallback.
